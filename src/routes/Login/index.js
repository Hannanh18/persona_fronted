import React from 'react';
import Layout from '../../components/Layout/Layout';
import LoginView from './LoginView';

export default {
  component: function() {
    return (
      <Layout>
        <LoginView />
      </Layout>
    )
  }
};
